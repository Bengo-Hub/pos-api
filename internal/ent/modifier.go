// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/pos-service/internal/ent/modifier"
	"github.com/bengobox/pos-service/internal/ent/modifiergroup"
	"github.com/google/uuid"
)

// Modifier is the model entity for the Modifier schema.
type Modifier struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ModifierGroupID holds the value of the "modifier_group_id" field.
	ModifierGroupID uuid.UUID `json:"modifier_group_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// PriceOverride holds the value of the "price_override" field.
	PriceOverride *float64 `json:"price_override,omitempty"`
	// IsAvailable holds the value of the "is_available" field.
	IsAvailable bool `json:"is_available,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ModifierQuery when eager-loading is set.
	Edges        ModifierEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ModifierEdges holds the relations/edges for other nodes in the graph.
type ModifierEdges struct {
	// Group holds the value of the group edge.
	Group *ModifierGroup `json:"group,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// GroupOrErr returns the Group value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ModifierEdges) GroupOrErr() (*ModifierGroup, error) {
	if e.Group != nil {
		return e.Group, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: modifiergroup.Label}
	}
	return nil, &NotLoadedError{edge: "group"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Modifier) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case modifier.FieldIsAvailable:
			values[i] = new(sql.NullBool)
		case modifier.FieldPriceOverride:
			values[i] = new(sql.NullFloat64)
		case modifier.FieldName:
			values[i] = new(sql.NullString)
		case modifier.FieldCreatedAt, modifier.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case modifier.FieldID, modifier.FieldModifierGroupID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Modifier fields.
func (_m *Modifier) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case modifier.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case modifier.FieldModifierGroupID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field modifier_group_id", values[i])
			} else if value != nil {
				_m.ModifierGroupID = *value
			}
		case modifier.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case modifier.FieldPriceOverride:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field price_override", values[i])
			} else if value.Valid {
				_m.PriceOverride = new(float64)
				*_m.PriceOverride = value.Float64
			}
		case modifier.FieldIsAvailable:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_available", values[i])
			} else if value.Valid {
				_m.IsAvailable = value.Bool
			}
		case modifier.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case modifier.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Modifier.
// This includes values selected through modifiers, order, etc.
func (_m *Modifier) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryGroup queries the "group" edge of the Modifier entity.
func (_m *Modifier) QueryGroup() *ModifierGroupQuery {
	return NewModifierClient(_m.config).QueryGroup(_m)
}

// Update returns a builder for updating this Modifier.
// Note that you need to call Modifier.Unwrap() before calling this method if this Modifier
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Modifier) Update() *ModifierUpdateOne {
	return NewModifierClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Modifier entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Modifier) Unwrap() *Modifier {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Modifier is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Modifier) String() string {
	var builder strings.Builder
	builder.WriteString("Modifier(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("modifier_group_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ModifierGroupID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	if v := _m.PriceOverride; v != nil {
		builder.WriteString("price_override=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("is_available=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsAvailable))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Modifiers is a parsable slice of Modifier.
type Modifiers []*Modifier
