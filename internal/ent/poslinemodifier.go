// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/pos-service/internal/ent/poslinemodifier"
	"github.com/bengobox/pos-service/internal/ent/posorderline"
	"github.com/google/uuid"
)

// POSLineModifier is the model entity for the POSLineModifier schema.
type POSLineModifier struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// LineID holds the value of the "line_id" field.
	LineID uuid.UUID `json:"line_id,omitempty"`
	// ModifierID holds the value of the "modifier_id" field.
	ModifierID uuid.UUID `json:"modifier_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// PriceApplied holds the value of the "price_applied" field.
	PriceApplied float64 `json:"price_applied,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the POSLineModifierQuery when eager-loading is set.
	Edges        POSLineModifierEdges `json:"edges"`
	selectValues sql.SelectValues
}

// POSLineModifierEdges holds the relations/edges for other nodes in the graph.
type POSLineModifierEdges struct {
	// Line holds the value of the line edge.
	Line *POSOrderLine `json:"line,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// LineOrErr returns the Line value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e POSLineModifierEdges) LineOrErr() (*POSOrderLine, error) {
	if e.Line != nil {
		return e.Line, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: posorderline.Label}
	}
	return nil, &NotLoadedError{edge: "line"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*POSLineModifier) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case poslinemodifier.FieldPriceApplied:
			values[i] = new(sql.NullFloat64)
		case poslinemodifier.FieldName:
			values[i] = new(sql.NullString)
		case poslinemodifier.FieldID, poslinemodifier.FieldLineID, poslinemodifier.FieldModifierID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the POSLineModifier fields.
func (_m *POSLineModifier) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case poslinemodifier.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case poslinemodifier.FieldLineID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field line_id", values[i])
			} else if value != nil {
				_m.LineID = *value
			}
		case poslinemodifier.FieldModifierID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field modifier_id", values[i])
			} else if value != nil {
				_m.ModifierID = *value
			}
		case poslinemodifier.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case poslinemodifier.FieldPriceApplied:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field price_applied", values[i])
			} else if value.Valid {
				_m.PriceApplied = value.Float64
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the POSLineModifier.
// This includes values selected through modifiers, order, etc.
func (_m *POSLineModifier) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryLine queries the "line" edge of the POSLineModifier entity.
func (_m *POSLineModifier) QueryLine() *POSOrderLineQuery {
	return NewPOSLineModifierClient(_m.config).QueryLine(_m)
}

// Update returns a builder for updating this POSLineModifier.
// Note that you need to call POSLineModifier.Unwrap() before calling this method if this POSLineModifier
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *POSLineModifier) Update() *POSLineModifierUpdateOne {
	return NewPOSLineModifierClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the POSLineModifier entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *POSLineModifier) Unwrap() *POSLineModifier {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: POSLineModifier is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *POSLineModifier) String() string {
	var builder strings.Builder
	builder.WriteString("POSLineModifier(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("line_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.LineID))
	builder.WriteString(", ")
	builder.WriteString("modifier_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ModifierID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("price_applied=")
	builder.WriteString(fmt.Sprintf("%v", _m.PriceApplied))
	builder.WriteByte(')')
	return builder.String()
}

// POSLineModifiers is a parsable slice of POSLineModifier.
type POSLineModifiers []*POSLineModifier
