// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/pos-service/internal/ent/inventorysnapshot"
	"github.com/google/uuid"
)

// InventorySnapshot is the model entity for the InventorySnapshot schema.
type InventorySnapshot struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// OutletID holds the value of the "outlet_id" field.
	OutletID uuid.UUID `json:"outlet_id,omitempty"`
	// CatalogItemID holds the value of the "catalog_item_id" field.
	CatalogItemID uuid.UUID `json:"catalog_item_id,omitempty"`
	// QuantityOnHand holds the value of the "quantity_on_hand" field.
	QuantityOnHand float64 `json:"quantity_on_hand,omitempty"`
	// CapturedAt holds the value of the "captured_at" field.
	CapturedAt   time.Time `json:"captured_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*InventorySnapshot) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case inventorysnapshot.FieldQuantityOnHand:
			values[i] = new(sql.NullFloat64)
		case inventorysnapshot.FieldCapturedAt:
			values[i] = new(sql.NullTime)
		case inventorysnapshot.FieldID, inventorysnapshot.FieldTenantID, inventorysnapshot.FieldOutletID, inventorysnapshot.FieldCatalogItemID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the InventorySnapshot fields.
func (_m *InventorySnapshot) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case inventorysnapshot.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case inventorysnapshot.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				_m.TenantID = *value
			}
		case inventorysnapshot.FieldOutletID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field outlet_id", values[i])
			} else if value != nil {
				_m.OutletID = *value
			}
		case inventorysnapshot.FieldCatalogItemID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field catalog_item_id", values[i])
			} else if value != nil {
				_m.CatalogItemID = *value
			}
		case inventorysnapshot.FieldQuantityOnHand:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity_on_hand", values[i])
			} else if value.Valid {
				_m.QuantityOnHand = value.Float64
			}
		case inventorysnapshot.FieldCapturedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field captured_at", values[i])
			} else if value.Valid {
				_m.CapturedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the InventorySnapshot.
// This includes values selected through modifiers, order, etc.
func (_m *InventorySnapshot) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this InventorySnapshot.
// Note that you need to call InventorySnapshot.Unwrap() before calling this method if this InventorySnapshot
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *InventorySnapshot) Update() *InventorySnapshotUpdateOne {
	return NewInventorySnapshotClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the InventorySnapshot entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *InventorySnapshot) Unwrap() *InventorySnapshot {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: InventorySnapshot is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *InventorySnapshot) String() string {
	var builder strings.Builder
	builder.WriteString("InventorySnapshot(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TenantID))
	builder.WriteString(", ")
	builder.WriteString("outlet_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.OutletID))
	builder.WriteString(", ")
	builder.WriteString("catalog_item_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.CatalogItemID))
	builder.WriteString(", ")
	builder.WriteString("quantity_on_hand=")
	builder.WriteString(fmt.Sprintf("%v", _m.QuantityOnHand))
	builder.WriteString(", ")
	builder.WriteString("captured_at=")
	builder.WriteString(_m.CapturedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// InventorySnapshots is a parsable slice of InventorySnapshot.
type InventorySnapshots []*InventorySnapshot
