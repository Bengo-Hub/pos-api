// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/pos-service/internal/ent/repairjob"
	"github.com/bengobox/pos-service/internal/ent/repairjobevent"
	"github.com/google/uuid"
)

// RepairJobEvent is the model entity for the RepairJobEvent schema.
type RepairJobEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// RepairJobID holds the value of the "repair_job_id" field.
	RepairJobID uuid.UUID `json:"repair_job_id,omitempty"`
	// intake, diagnosis, parts_added, status_change, note, settled
	EventType string `json:"event_type,omitempty"`
	// Notes holds the value of the "notes" field.
	Notes string `json:"notes,omitempty"`
	// ActorID holds the value of the "actor_id" field.
	ActorID *uuid.UUID `json:"actor_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RepairJobEventQuery when eager-loading is set.
	Edges        RepairJobEventEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RepairJobEventEdges holds the relations/edges for other nodes in the graph.
type RepairJobEventEdges struct {
	// RepairJob holds the value of the repair_job edge.
	RepairJob *RepairJob `json:"repair_job,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// RepairJobOrErr returns the RepairJob value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RepairJobEventEdges) RepairJobOrErr() (*RepairJob, error) {
	if e.RepairJob != nil {
		return e.RepairJob, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: repairjob.Label}
	}
	return nil, &NotLoadedError{edge: "repair_job"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RepairJobEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case repairjobevent.FieldActorID:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case repairjobevent.FieldEventType, repairjobevent.FieldNotes:
			values[i] = new(sql.NullString)
		case repairjobevent.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case repairjobevent.FieldID, repairjobevent.FieldRepairJobID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RepairJobEvent fields.
func (_m *RepairJobEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case repairjobevent.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case repairjobevent.FieldRepairJobID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field repair_job_id", values[i])
			} else if value != nil {
				_m.RepairJobID = *value
			}
		case repairjobevent.FieldEventType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field event_type", values[i])
			} else if value.Valid {
				_m.EventType = value.String
			}
		case repairjobevent.FieldNotes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field notes", values[i])
			} else if value.Valid {
				_m.Notes = value.String
			}
		case repairjobevent.FieldActorID:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field actor_id", values[i])
			} else if value.Valid {
				_m.ActorID = new(uuid.UUID)
				*_m.ActorID = *value.S.(*uuid.UUID)
			}
		case repairjobevent.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RepairJobEvent.
// This includes values selected through modifiers, order, etc.
func (_m *RepairJobEvent) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryRepairJob queries the "repair_job" edge of the RepairJobEvent entity.
func (_m *RepairJobEvent) QueryRepairJob() *RepairJobQuery {
	return NewRepairJobEventClient(_m.config).QueryRepairJob(_m)
}

// Update returns a builder for updating this RepairJobEvent.
// Note that you need to call RepairJobEvent.Unwrap() before calling this method if this RepairJobEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RepairJobEvent) Update() *RepairJobEventUpdateOne {
	return NewRepairJobEventClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RepairJobEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RepairJobEvent) Unwrap() *RepairJobEvent {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RepairJobEvent is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RepairJobEvent) String() string {
	var builder strings.Builder
	builder.WriteString("RepairJobEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("repair_job_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.RepairJobID))
	builder.WriteString(", ")
	builder.WriteString("event_type=")
	builder.WriteString(_m.EventType)
	builder.WriteString(", ")
	builder.WriteString("notes=")
	builder.WriteString(_m.Notes)
	builder.WriteString(", ")
	if v := _m.ActorID; v != nil {
		builder.WriteString("actor_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RepairJobEvents is a parsable slice of RepairJobEvent.
type RepairJobEvents []*RepairJobEvent
