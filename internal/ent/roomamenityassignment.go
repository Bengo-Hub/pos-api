// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/bengobox/pos-service/internal/ent/room"
	"github.com/bengobox/pos-service/internal/ent/roomamenity"
	"github.com/bengobox/pos-service/internal/ent/roomamenityassignment"
	"github.com/google/uuid"
)

// RoomAmenityAssignment is the model entity for the RoomAmenityAssignment schema.
type RoomAmenityAssignment struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID uuid.UUID `json:"tenant_id,omitempty"`
	// RoomID holds the value of the "room_id" field.
	RoomID uuid.UUID `json:"room_id,omitempty"`
	// AmenityID holds the value of the "amenity_id" field.
	AmenityID uuid.UUID `json:"amenity_id,omitempty"`
	// true = included in room rate; false = charged separately
	IsIncluded bool `json:"is_included,omitempty"`
	// Notes holds the value of the "notes" field.
	Notes string `json:"notes,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RoomAmenityAssignmentQuery when eager-loading is set.
	Edges        RoomAmenityAssignmentEdges `json:"edges"`
	selectValues sql.SelectValues
}

// RoomAmenityAssignmentEdges holds the relations/edges for other nodes in the graph.
type RoomAmenityAssignmentEdges struct {
	// Room holds the value of the room edge.
	Room *Room `json:"room,omitempty"`
	// Amenity holds the value of the amenity edge.
	Amenity *RoomAmenity `json:"amenity,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// RoomOrErr returns the Room value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RoomAmenityAssignmentEdges) RoomOrErr() (*Room, error) {
	if e.Room != nil {
		return e.Room, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: room.Label}
	}
	return nil, &NotLoadedError{edge: "room"}
}

// AmenityOrErr returns the Amenity value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RoomAmenityAssignmentEdges) AmenityOrErr() (*RoomAmenity, error) {
	if e.Amenity != nil {
		return e.Amenity, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: roomamenity.Label}
	}
	return nil, &NotLoadedError{edge: "amenity"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RoomAmenityAssignment) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case roomamenityassignment.FieldIsIncluded:
			values[i] = new(sql.NullBool)
		case roomamenityassignment.FieldNotes:
			values[i] = new(sql.NullString)
		case roomamenityassignment.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case roomamenityassignment.FieldID, roomamenityassignment.FieldTenantID, roomamenityassignment.FieldRoomID, roomamenityassignment.FieldAmenityID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RoomAmenityAssignment fields.
func (_m *RoomAmenityAssignment) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case roomamenityassignment.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case roomamenityassignment.FieldTenantID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value != nil {
				_m.TenantID = *value
			}
		case roomamenityassignment.FieldRoomID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field room_id", values[i])
			} else if value != nil {
				_m.RoomID = *value
			}
		case roomamenityassignment.FieldAmenityID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field amenity_id", values[i])
			} else if value != nil {
				_m.AmenityID = *value
			}
		case roomamenityassignment.FieldIsIncluded:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_included", values[i])
			} else if value.Valid {
				_m.IsIncluded = value.Bool
			}
		case roomamenityassignment.FieldNotes:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field notes", values[i])
			} else if value.Valid {
				_m.Notes = value.String
			}
		case roomamenityassignment.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RoomAmenityAssignment.
// This includes values selected through modifiers, order, etc.
func (_m *RoomAmenityAssignment) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryRoom queries the "room" edge of the RoomAmenityAssignment entity.
func (_m *RoomAmenityAssignment) QueryRoom() *RoomQuery {
	return NewRoomAmenityAssignmentClient(_m.config).QueryRoom(_m)
}

// QueryAmenity queries the "amenity" edge of the RoomAmenityAssignment entity.
func (_m *RoomAmenityAssignment) QueryAmenity() *RoomAmenityQuery {
	return NewRoomAmenityAssignmentClient(_m.config).QueryAmenity(_m)
}

// Update returns a builder for updating this RoomAmenityAssignment.
// Note that you need to call RoomAmenityAssignment.Unwrap() before calling this method if this RoomAmenityAssignment
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *RoomAmenityAssignment) Update() *RoomAmenityAssignmentUpdateOne {
	return NewRoomAmenityAssignmentClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the RoomAmenityAssignment entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *RoomAmenityAssignment) Unwrap() *RoomAmenityAssignment {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: RoomAmenityAssignment is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *RoomAmenityAssignment) String() string {
	var builder strings.Builder
	builder.WriteString("RoomAmenityAssignment(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("tenant_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.TenantID))
	builder.WriteString(", ")
	builder.WriteString("room_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.RoomID))
	builder.WriteString(", ")
	builder.WriteString("amenity_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AmenityID))
	builder.WriteString(", ")
	builder.WriteString("is_included=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsIncluded))
	builder.WriteString(", ")
	builder.WriteString("notes=")
	builder.WriteString(_m.Notes)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RoomAmenityAssignments is a parsable slice of RoomAmenityAssignment.
type RoomAmenityAssignments []*RoomAmenityAssignment
