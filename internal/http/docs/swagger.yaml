basePath: /api/v1
info:
  contact: {}
  description: HTTP API for the BengoBox POS service. Provides point-of-sale operations,
    order management, and cash drawer management.
  title: POS Service API
  version: 0.1.0
paths: {}
schemes:
- http
- https
securityDefinitions:
  bearerAuth:
    description: 'JWT token from auth-service. Format: Bearer {token}'
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
